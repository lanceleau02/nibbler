# nibbler

## How to build and run the program

**Build and install:**

- `make setup`
- `make`

**Launch:**

`./nibbler <width> <height>` or `make run`

**Rebuild:**

`make re`

**Build and run the docker:**

- `make docker-build`
- `make docker-run`
